using UnityEngine;
using System.Collections;

public class InfoBoxes : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GUI.TextArea(new Rect(0, 0, 0, 0), "Hello world!");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
